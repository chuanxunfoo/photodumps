/**
 * Metro stub for Expo Go — react-native-nitro-modules is not available in StoreClient.
 */

const noop = () => {};

export default {
  install: noop,
};

export const NitroModules = {
  install: noop,
};
