# Copy this file to ".env" in the same folder as package.json (project root).
# Values: Supabase Dashboard → Project Settings → API
# After saving, stop Metro completely and run: npx expo start -c

EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_PUBLIC_KEY

# Sticker Studio — Expo Go on iPhone (while waiting for Apple Dev): add remove.bg key for ~5–15s cutouts.
# Free tier: https://www.remove.bg/api (limited credits/month).
# Dev / store builds: EAS sets NATIVE_CUTOUT=1 automatically.
EXPO_PUBLIC_REMOVE_BG_API_KEY=
# 0 for Expo Go. Set 1 only when using npm run ios / android / start:dev-client (not --go).
EXPO_PUBLIC_NATIVE_CUTOUT=0
